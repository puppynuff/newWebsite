//* Shiro, 12,25,23
// Work on custructing the window, the rest will be handled by the window developer.
// Dont worry, it should end up being pretty simple to create a window.

class RWM_Window {
    /**
     * 
     * @param {string} name 
     */
    constructor(name) {
        this.name = name;
        this.createWindow();
        console.log(name);
    }


    createWindow() {
        //TODO Create create the window.
    }
}